pipeline {
    agent any
    stages {
        stage('Build Image') {
            steps {
                sh 'echo "Building some image"'
            }
        }

        stage('Reminder') {
            steps {
                sh 'echo "so to run the commands, we have to use quotes"'
            }
        }
    }
}